export interface Ciudad {
  nombre: string;
  timeZone: string;
  descripcion: string;
  timeDate: Date;
  time?: string;
  noche?: boolean;
  emoji: string;
  country: string;
  favorita?: boolean;
}
